---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarr
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://dakoel.github.io/helm-charts'
    chart: sonarr
    targetRevision: {{ .Values.sonarr.targetRevision }}
    helm:
      values: |
        ingress:
          enabled: true
          className: traefik
          hosts:
            - host: sonarr.{{ .Values.domain }}
              paths:
                - path: /
                  pathType: Prefix
  destination:
    server: {{ .Values.server }}
    namespace: media-stack
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
